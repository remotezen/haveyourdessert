<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:recipes, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:recipes, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :recipe, url(:recipes, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'recipes/form', :locals => { :f => f } %>
 <% end %>
</div>
